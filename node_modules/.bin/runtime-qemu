#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../runtime-tools/bin/runtime-qemu.js" "$@"
  ret=$?
else 
  node  "$basedir/../runtime-tools/bin/runtime-qemu.js" "$@"
  ret=$?
fi
exit $ret
