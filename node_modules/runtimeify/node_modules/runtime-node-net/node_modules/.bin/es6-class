#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../es6-class/bin/es6-class" "$@"
  ret=$?
else 
  node  "$basedir/../es6-class/bin/es6-class" "$@"
  ret=$?
fi
exit $ret
